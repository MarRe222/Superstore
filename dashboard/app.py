"""
The entry point and app instance
This is where the Dash app object is created (app = Dash(__name__)) 
and where the server is launched (app.run(...)). 
It ties everything together by importing the layout and registering the callbacks. 
It's intentionally kept minimal — think of it as the "main" file for your dashboard.
"""